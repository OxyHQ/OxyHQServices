/**
 * Re-exports Bloom's useTheme so existing imports keep working.
 * Prefer importing useTheme directly from '@oxyhq/bloom/theme' in new code.
 */
export { useTheme } from '@oxyhq/bloom/theme';
export type { Theme, ThemeColors } from '@oxyhq/bloom/theme';
