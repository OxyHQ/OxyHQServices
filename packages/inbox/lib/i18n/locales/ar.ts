// Arabic (ar-SA) — placeholder; falls back to English until populated.
// When translating, copy `en.ts` and translate values in place.
// Note: Arabic is right-to-left; RTL layout support is not yet wired in
// the inbox app and should be added when this locale is populated.
import en from './en';
export default en;
