/**
 * Build-time global injected by Vite's `define` (see vite.config.ts).
 * Sourced from `public/manifest.json` `short_name` — the single canonical
 * home for the app display name. Never hardcode the name elsewhere.
 */
declare const __APP_NAME__: string;
